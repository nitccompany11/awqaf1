<section class="page-info-bar">
        <div class="container">
            <h1>
                {{ $title }}
            </h1>
            <h4>
                <span class="text-white-50">{{ trans('home.home') }}</span> &gt;
                <span>{{ $title }}</span>
            </h4>
        </div>
    </section>